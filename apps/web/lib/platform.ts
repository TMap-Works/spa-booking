/**
 * L'identité de la plateforme elle-même — pas celle d'un salon (#927).
 *
 * Trois écrans la nomment : la page d'accueil, et les deux écrans de connexion,
 * qui renvoient vers elle. Le nom est écrit ici une fois, pour qu'un changement
 * de marque ne se fasse pas écran par écran.
 */
export const PLATFORM_NAME = 'Spa & Salon Booking';

/** La page d'accueil de la plateforme — la racine du domaine. */
export const PLATFORM_HOME_PATH = '/';
